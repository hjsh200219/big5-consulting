# Tech Debt Tracker -- Big 5 Consulting MCP Server

최종 감사: 2026-03-27

## P0 -- 즉각 조치 필요

### TD-001: 테스트 전무
- **위치**: 프로젝트 전체
- **현재 상태**: `package.json` test 스크립트가 placeholder (`echo "Error"`)
- **영향**: 코드 변경 시 회귀 검증 불가
- **권장 조치**:
  1. Jest 또는 Vitest 도입
  2. `calculateScores()` 순수 함수 단위 테스트 작성 (우선)
  3. StorageManager CRUD 단위 테스트 작성
  4. 각 도구 `handle()` 통합 테스트 작성
- **예상 공수**: 2-3일

## P1 -- 중요하지만 긴급하지 않음

### TD-002: `any` 타입 남용
- **위치**: `src/tools/*.ts` 전체
- **현재 상태**: `handle()` 반환 타입이 `Promise<any>`, 분석 메서드 내부 `any` 다수
- **영향**: 타입 안전성 저하, 리팩토링 시 에러 감지 불가
- **권장 조치**:
  1. 각 분석 타입별 반환 인터페이스 정의 (`types/index.ts`)
  2. `handle()` 반환 타입을 구체적 union 타입으로 교체
  3. `options` 파라미터를 구체적 인터페이스로 교체
- **예상 공수**: 1-2일

### TD-003: 에러 메시지 한글/영문 혼재
- **위치**: `src/tools/*.ts`, `src/services/storage.ts`
- **현재 상태**: 일부 에러 메시지가 영문, 일부가 한글
- **영향**: 사용자 경험 일관성 저하
- **권장 조치**: 모든 에러 메시지를 한글로 통일
- **예상 공수**: 0.5일

## P2 -- 구조적 개선

### TD-004: 분석 도구 과대 복잡도
- **위치**: `src/tools/analyzeTeam.ts` (509줄), `src/tools/analyzeRelationship.ts` (512줄)
- **현재 상태**: 통계 계산, 결과 포맷팅, 추천 생성이 단일 파일에 혼재
- **영향**: 유지보수 어려움, 새로운 분석 타입 추가 시 복잡도 증가
- **권장 조치**:
  1. 통계/계산 유틸리티를 `src/utils/` 디렉토리로 분리
  2. 분석 규칙을 설정 객체로 외부화
  3. 결과 포맷팅 로직 분리
- **예상 공수**: 2-3일

## P3 -- 향후 개선

### TD-005: 하드코딩된 분석 규칙
- **위치**: `src/tools/analyze*.ts`
- **현재 상태**: 임계값, 분류 기준 등이 코드에 직접 하드코딩
- **영향**: 분석 규칙 변경 시 코드 수정 필요
- **권장 조치**: 분석 규칙/임계값을 `src/data/rules.ts` 등 설정 파일로 외부화
- **예상 공수**: 1-2일

### TD-006: 커스텀 에러 클래스 부재
- **위치**: 프로젝트 전체
- **현재 상태**: 모든 에러가 generic `Error` 클래스
- **영향**: 에러 분류/처리가 메시지 문자열에 의존
- **권장 조치**: `ProfileNotFoundError`, `InvalidSessionError` 등 도메인별 에러 클래스 정의
- **예상 공수**: 0.5일

## 완료된 항목

(아직 완료된 항목 없음)
